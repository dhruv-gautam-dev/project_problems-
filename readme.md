i have created two project in react project

1. react app project
2. create react component
   2.1 create first component

in (react app project ){
description component is not working
and kgbutton component is working
}

in (createReactComp\createFirstComp ){
description component is not working
and subscribe component is working
}
